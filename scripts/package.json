{
  "name": "scripts",
  "version": "1.0.0",
  "description": "",
  "main": "run_deploy",
  "scripts": {
    "deploy": "node run_deploy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/exec": "^1.0.4",
    "@actions/github": "^4.0.0"
  }
}
